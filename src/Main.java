/**
 * Entry point. Its only job is to launch the welcome screen —
 * it holds no game logic or UI of its own.
 */
public class Main {
    public static void main(String[] args) {
        new WelcomeScreen();
    }
}
